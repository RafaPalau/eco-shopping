/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["useorganico.vtexassets.com", "example.com"], // Colocar aqui os domínios das imagens que você deseja carregar
  },
};

module.exports = nextConfig;
